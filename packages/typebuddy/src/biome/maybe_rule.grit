or {
  // T | null -> Maybe<T>
  `$type | null` where {
    $type <: not contains `null | undefined`
  } => `Maybe<$type>`,

  // T | null | undefined -> Nullable<T>
  `$type | null | undefined` where {
    $type <: not contains `null | undefined`
  } => `Nullable<$type>`
}
