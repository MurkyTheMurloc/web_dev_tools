---
"@murky-web/typebuddy": minor
---

Add `ok()` and `err()` result helpers for MaybePromise flows, improve async rule handling for framework callbacks, and align generated fixes with inline result objects.
