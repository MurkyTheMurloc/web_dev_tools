# Changesets

This directory stores release notes and versioning intents for the `web_kit`
workspace.

Run `bun run changeset` to create a new changeset for a package change. The
release workflow will turn pending changesets into a version PR on `master`.
