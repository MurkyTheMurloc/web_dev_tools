---
"@murky-web/typebuddy": patch
---

Prefer `ok(...)` and `err()` in TypeBuddy autofixes, including the MaybePromise and async try-catch lint rules. This also improves the fix smoke coverage for multi-pass Oxlint JS-plugin behavior.
